# Python bytecode / test caches (generated locally, not part of the tool)
__pycache__/
*.py[cod]
.pytest_cache/

# Preview output from report_sample.py and ui_preview.py, and the intermediate
# paginated document
sample-report.html
sample-report.pdf
preview/
*.print.html
vendor/pagedjs/*.js

# Local virtualenv, the generated domain-verification secret, and the saved
# scan presets (per browser session; scan settings only, never credentials)
.venv/
.verify_secret
presets/
